package com.example.person.db.repository.mongodb;

import com.example.person.annotation.env.AutoScanPackage;
/*

This is an empty package interface for package-marking
It is places in the packages to be scanned
Used inside Java Spring configuration to mark a package as java compile safe and not as a string

*/

@AutoScanPackage
public interface RepositoryPackageMarker {
}
